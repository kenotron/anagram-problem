import dictionary from "./words.json";

// Given dictinoary which is an array of English words, list out all the anagrams for each of the words
// Anagram example: act, cat, tac
console.log(dictionary);
